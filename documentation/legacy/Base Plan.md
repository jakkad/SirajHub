# SirajHub — Personal Content Tracking Hub — Plan V1

## Context
Building a personal media/content tracker for Books, Movies, TV Shows, Podcasts, YouTube, Articles, and Tweets. Items tracked across four statuses: Suggestions, In Progress, Finished, Archived. AI auto-categorizes new items and optionally generates summaries + a ranked "next to consume" list. Runs free on Cloudflare Workers (shared hosting not used — Workers replaces it entirely).

---

## Tech Stack

| Layer | Choice | Why |
|---|---|---|
| Frontend | React 19 + Vite + TypeScript | Widest shadcn/ui ecosystem; scales to team/public later |
| Routing | TanStack Router | File-based, fully type-safe SPA routing |
| Server state | TanStack Query v5 | Cache, background refetch, optimistic updates |
| Styling | Tailwind CSS v4 + shadcn/ui | CSS-first config, OKLCH dark palette, owned components |
| Drag and drop | @dnd-kit/core | Kanban board between status columns |
| API Framework | Hono v4 on Cloudflare Workers | Workers-native, typed RPC, ultra-lightweight |
| ORM | Drizzle ORM | Best D1 support, type-safe migrations |
| Database | Cloudflare D1 (SQLite) | Free: 5M reads/100k writes per day |
| Cache | Cloudflare KV | Sessions + URL metadata cache (24h TTL) |
| Auth | Better Auth | Native D1 + KV support, no third-party server |
| AI | Gemini 2.5 Flash-Lite | Free 1,000 req/day; 1M context window |
| Monorepo | pnpm workspaces | `apps/web` + `worker` packages |
| Deploy | Single `wrangler deploy` | Ships SPA static assets + Worker API together |

---

## Architecture

```
Browser (React SPA — served from Workers Static Assets)
    │  Hono RPC types (end-to-end type-safe)
    ▼
Cloudflare Worker (Hono v4)
 ├── /api/items       CRUD operations
 ├── /api/ingest      URL → metadata fetcher dispatcher
 ├── /api/ai          summarize / rank / categorize
 └── /auth/*          Better Auth handler
    │
    ├── D1 Database (Drizzle)
    ├── KV Store (sessions + URL cache)
    └── Gemini API (outbound fetch — no CPU cost)
            │
            └── External Metadata APIs
                 Books: Open Library + Google Books
                 Movies/TV: TMDB (free key)
                 Podcasts: Podcast Index + RSS parsing
                 YouTube: YouTube Data API v3
                 Articles: OG tag scraping (self-hosted)
                 Tweets: Twitter oEmbed (no key)
```

---

## Project Structure

```
sirajhub/
├── package.json                  # pnpm root workspace
├── pnpm-workspace.yaml
├── wrangler.toml                 # D1/KV bindings, static assets dir, routes
├── .github/workflows/deploy.yml  # GitHub Actions → wrangler deploy
│
├── apps/web/                     # React SPA
│   ├── vite.config.ts            # @cloudflare/vite-plugin
│   └── src/
│       ├── routes/               # TanStack Router: index, item.$id, settings
│       ├── components/
│       │   ├── ui/               # shadcn copied components
│       │   ├── ItemCard.tsx
│       │   ├── BoardView.tsx     # Kanban 4-column layout
│       │   ├── GridView.tsx      # Masonry card grid
│       │   ├── AddItemDialog.tsx # URL/title search → ingest
│       │   └── AiSummaryPanel.tsx
│       ├── lib/api.ts            # Hono RPC client (type-safe)
│       └── hooks/                # useItems, useAI
│
└── worker/                       # Hono API
    └── src/
        ├── index.ts              # app entry + route mounting
        ├── routes/               # items, ingest, ai, auth
        ├── services/
        │   ├── metadata/         # index.ts dispatcher + per-type fetchers
        │   ├── ai.ts             # Gemini API calls + prompt builders
        │   └── cache.ts          # KV helpers
        ├── db/
        │   ├── schema.ts         # Drizzle schema (source of truth)
        │   └── migrations/       # generated by drizzle-kit
        └── auth.ts               # Better Auth instance
```

---

## Database Schema (D1 / SQLite via Drizzle)

**Tables:**
- `users` — id (ulid), email, name, preferences (JSON: taste profile for AI)
- `sessions` — Better Auth managed
- `items` — id, user_id, content_type, status, source_url, external_id, title, creator, description, cover_url, release_date, duration_mins, metadata (JSON blob for type-specific extras), position (drag order), rating, notes, started_at, finished_at
- `tags` + `item_tags` — many-to-many, user-scoped
- `ai_cache` — content_id, analysis_type, model_used, prompt_hash, result (JSON), created_at
- `url_cache` — url (PK), metadata (JSON), fetched_at, source

**Design decisions:**
- `metadata TEXT` JSON blob on items (avoids 7 type-specific tables; fast enough for personal ~500-item scale)
- ULIDs throughout (sortable, URL-safe)
- Unix timestamps (integers) for all dates
- `position INTEGER` enables drag-and-drop reordering without touching every row

---

## AI Pipeline

### Auto-categorize on add (every new item)
Lightweight call to Gemini Flash-Lite:
```
Input: title + description + url domain
Output JSON: { content_type, confidence, suggested_tags, suggested_status }
```

### On-demand analysis (user clicks "Analyze")
1. Check D1 `ai_cache` → return if < 7 days old
2. Build prompt by content type (book → bullets+rating, article → TL;DR, movie → digest+mood)
3. Gemini Flash-Lite call with structured JSON output schema
4. Store in `ai_cache`, return to client

### "Next to Watch/Read" ranked list
1. Pull all `status = 'suggestions'` items from D1
2. Bundle into single prompt with user's taste preferences
3. Gemini ranks + gives 1-line reasoning per item
4. Cache result in KV for 6 hours

**Free tier budget:** Gemini 1k req/day vs. ~20–30 personal daily calls. Never hits the limit.

---

## Free Data Source APIs

| Type | API | Key Required |
|---|---|---|
| Books | Open Library API | No |
| Books (fallback) | Google Books API | Yes (free) |
| Movies/TV | TMDB API | Yes (free) |
| Podcasts | Podcast Index API | Yes (free) |
| Podcasts (fallback) | Apple iTunes Search | No |
| YouTube | YouTube Data API v3 (10k units/day) | Yes (free) |
| Articles | Self-hosted OG tag scraper in Worker | No |
| Tweets | Twitter oEmbed API | No |

URL dispatcher (`services/metadata/index.ts`) detects content type from URL pattern before calling the right fetcher.

---

## Design System

- **Dark mode first** — class-based toggle, defaults to dark
- **Color palette** — OKLCH CSS variables: near-black blue-tinted background, indigo/violet accent
- **Views:** Board (Kanban 4-column with @dnd-kit drag-drop) + Grid (CSS columns masonry)
- **Content type badges** — colored pills per type
- **shadcn components:** Card, Badge, Dialog, Command (search/add), Tabs, DropdownMenu, Skeleton, Sonner (toasts)
- **Mobile:** single-column list; Board collapses to horizontal scroll

---

## Auth

- Better Auth with email+password (personal use = one user, register once)
- Sessions in D1 (persistent) + KV (fast lookup)
- HTTP-only cookie, 30-day sliding window
- Hono middleware injects `user_id` into context before every `/api/*` route
- **Public launch path:** enable registration + Google OAuth in Better Auth config (one-line each), all queries already scoped by `user_id`

---

## Free Tier Budget

| Service | Free Limit | Expected Usage (personal) |
|---|---|---|
| Workers requests | 100k/day | ~50–200/day |
| D1 reads | 5M/day | ~1k/day |
| D1 writes | 100k/day | ~50/day |
| KV reads | 100k/day | ~500/day |
| KV writes | 1k/day | ~50/day |
| Gemini Flash-Lite | 1k/day | ~10–30/day |
| YouTube API | 10k units/day | ~20 units/day |
| TMDB | No published limit | ~5–20/day |

---

## Implementation Order

1. ✅ **Foundation** — wrangler.toml, pnpm workspace, Vite+React scaffold, Hono skeleton, Drizzle schema + D1 binding, shadcn base setup
2. ✅ **Auth** — Better Auth, session middleware, login page
3. ✅ **Core CRUD** — items routes, Board view UI, manual add form, drag-and-drop status columns
4. ✅ **Ingest pipeline** — URL dispatcher + each metadata fetcher, KV cache layer
5. ✅ **AI features** — Gemini integration, auto-categorize on add, manual summarize, "next list"
6. ⬜ **Polish** — Grid view, tags, search/filter, settings page, responsive mobile

---

## What's Been Built (Phases 1–5)

### Phase 1 — Foundation
Full pnpm workspace monorepo (`apps/web` + `worker`). Vite + React 19 + TanStack Router SPA. Hono v4 worker skeleton. Drizzle schema covering all tables (`items`, `users`, `sessions`, `tags`, `item_tags`, `ai_cache`, `url_cache`). D1 + KV bindings wired in `wrangler.toml`. Tailwind v4 with OKLCH dark palette CSS variables. D1 migrations generated and applied.

### Phase 2 — Auth
Better Auth instance configured with email+password. D1 adapter for persistent sessions, KV for fast session lookup. HTTP-only cookie with 30-day sliding window. `requireAuth` Hono middleware injects `userId` into context for all `/api/*` routes. Login page with sign-in / sign-up toggle.

### Phase 3 — Core CRUD
Full REST items router (`GET /`, `POST /`, `PATCH /:id`, `DELETE /:id`) scoped by `userId`. Kanban `BoardView` with four status columns (Suggestions, In Progress, Finished, Archived). `ItemCard` with cover image, type badge, rating stars, and 3-dot action menu. Drag-and-drop between columns via `@dnd-kit/core`. `AddItemDialog` supports URL paste, name search, and full manual entry.

### Phase 4 — Ingest Pipeline
URL dispatcher (`services/metadata/index.ts`) detects content type from URL pattern and routes to the correct fetcher:
- **YouTube** — YouTube Data API v3 (title, thumbnail, channel, duration)
- **Movies/TV** — TMDB API (poster, genres, runtime, cast)
- **Books** — Open Library primary, Google Books fallback (cover, ISBN, page count)
- **Podcasts** — Podcast Index primary, Apple iTunes fallback (feed, episode count)
- **Articles** — self-hosted OG tag scraper in the Worker (title, description, site name)
- **Tweets** — Twitter oEmbed (author, embed HTML)
- **Name search** — routes `query` + `content_type` to the right API without a URL

24-hour `url_cache` in D1 prevents redundant external fetches. `AddItemDialog` pre-fills all form fields from the fetched metadata.

### Phase 5 — AI Features
Gemini API service (`services/ai.ts`) using `gemini-2.0-flash-lite` (free tier: 1,000 req/day). Three capabilities:

**Per-item analysis** (`POST /api/ai/analyze/:id`)
Generates a summary, 2–4 key points, a one-sentence recommendation, and an optional mood tag (movies/TV/books). Results stored in `ai_cache` (D1) with a 7-day TTL. Cache is automatically invalidated when the item is updated. In the UI: "Analyze" option in the ItemCard 3-dot menu; analysis expands inline on the card.

**"Next to Consume" ranked list** (`GET /api/ai/next`)
Pulls all `status = 'suggestions'` items and asks Gemini to rank them best-first, factoring in the user's taste preferences (stored in `user.preferences`). Each ranked entry includes a one-sentence reason. Result cached in KV for 6 hours; `?refresh=1` forces a fresh ranking. In the UI: "✨ Next to Consume" button in the nav bar opens a modal with the ranked list and a Refresh button.

**Auto-categorize** (`services/ai.ts → categorizeItem`)
Lightweight Gemini call available as a utility — confirms/corrects `content_type` and suggests tags from title + description + URL domain. Ready to be wired into any flow that needs it (e.g. manual adds in Phase 6).

---

## Verification Plan

1. `pnpm dev` → Vite dev server with Worker runtime via `@cloudflare/vite-plugin` (local D1 + KV emulation)
2. Add a YouTube URL → verify title/thumbnail auto-populated
3. Add a book title → verify Open Library metadata returned
4. Click "Analyze" on an item → verify Gemini summary appears and is cached (second click is instant)
5. Drag item from Suggestions → In Progress → verify status updated in D1
6. `wrangler deploy` → live at custom domain via Cloudflare
7. Check Cloudflare dashboard: D1 query counts, KV reads, Worker invocations all within free tier
